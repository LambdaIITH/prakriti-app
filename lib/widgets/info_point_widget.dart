import 'package:flutter/material.dart';

class InfoPointWidget extends StatelessWidget {
  const InfoPointWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}